.popup
	= form_tag(subscribe_newsletter_path, remote: true) do
		.subscribe
			%h2 THE "BRIEFS" LIST
			%p For folks who want the big updates (expansion, child-focused, etc.) and not much else.
			.get-updates
				%label{for: 'email'}Get Briefs
				%fieldset
					= email_field_tag('email', '', class: 'field tall', id: 'email', placeholder: 'email address')
					%span.btn-arrow.submit
						= submit_tag ''
						%i.arrow-right
